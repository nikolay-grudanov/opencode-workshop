# Inventories — restore-missing-docs

> Scratch file. NOT committed. Used only to cite line ranges while writing docs.

## 1.1 Source module anchors

| File | LOC | Purpose |
|---|---|---|
| `src/index.ts` | 872 | CLI entry. Dispatches `raindrop workshop <verb>`, `setup`, `login`, `cloud`, `replay`, `update`, `uninstall`. Daemon launcher (`ensureDaemonRunning`, `src/index.ts:153-211`). |
| `src/server.ts` | 2550 | Express HTTP + `ws` WebSocket daemon. `createServer(port)` at `src/server.ts:410-2316`. |
| `src/db/schema.ts` | 171 | Drizzle schema. Tables: `runs`, `spans`, `live_events`, `saved_run_cache`, `saved_events`, `saved_folders`, `sessions`, `messages`, `annotations`, plus view `runs_with_hints`. |
| `src/db/migration-assets.ts` | (separate) | Embeds migration SQL into the daemon binary. |
| `src/spans/normalize.ts` | 95 | `normalizeSpan()` dispatcher (`src/spans/normalize.ts:42-48`). Iterates `ADAPTERS` array (`:32-40`). Returns `AdapterMatch`. |
| `src/spans/adapters/` | (dir) | SDK-specific adapters: ai-sdk, claude-agent-sdk, livekit, traceloop. Plus `normalized.ts` (typed view) and `types.ts`. |
| `src/spans/normalized.ts` | (separate) | Exports `NormalizedSpan` type and `emptyNormalized()`. |
| `src/agents.ts` | 130 | `detectSubAgents(spans)` at `src/agents.ts:44-130`. Pattern: `TOOL_CALL > LLM_GENERATION > TOOL_CALL` or `TOOL_CALL > agent.subagent`. Returns `SubAgent[]`. |
| `src/replay.ts` | 367 | Replay engine. `runReplay(config, res, broadcast)` exported at `src/replay.ts:356-362`. `runLocalAgentReplay` at `:180-354`. `ReplayConfig` interface at `:10-20`. |
| `src/parse.ts` | 242 | OTLP ingest → `ParsedSpan`. `parseOtlpRequest(body)` at `src/parse.ts:124-242`. `inferSpanType` at `:35-69`. Status mapping at `:95-108`. |
| `src/otlp-protobuf.ts` | 178 | Protobuf OTLP decoder. `decodeOtlpProtobuf(buf)` at `src/otlp-protobuf.ts:158-178`. OTLP proto definition at `:3-94`. |

## 1.2 HTTP route inventory (`src/server.ts`)

Ingest (OTLP / Raindrop SDK):
- `POST /v1/traces` `src/server.ts:822`
- `POST /v1/otel/v1/traces` `:823`
- `POST /otel/v1/traces` `:824`
- `POST /v1/live` `:826`
- `POST /v1/events/track_partial` `:878`
- `POST /v1/events/track` `:982`
- `POST /v1/users/identify` `:991`
- `POST /v1/signals/track` `:994`

System / UI:
- `GET /health` `:708`
- `GET /demo-chat` `:712`
- `POST /api/demo-chat` `:716`
- `POST /api/demo-traces/replay` `:728`
- `GET /api/ui/connected` `:1023`
- `GET /api/ui/viewing` `:1026`
- `GET /api/status` `:1754`
- `GET /api/clear` → actually `POST /api/clear` `:1328`
- `GET /api/directories` `:1372` (localOriginGuard)
- `GET /api/models/anthropic` `:1778`
- `POST /api/summarize` `:2061`

Runs / spans / trace queries:
- `GET /api/runs` `:998` (`limit` query)
- `GET /api/runs/active` `:1003`
- `POST /api/traces/query` `:1008` (SQL body)
- `GET /api/spans/:id` `:1174`
- `GET /api/spans/:id/payload` `:1186` (`target`, `jsonpath`, `max_chars`, `range`, `format`)
- `GET /api/spans/:id/context` `:1215` (`before`, `after`, `include_parent`)
- `GET /api/convo/:convoId` `:1237`
- `GET /api/runs/detail/:id` (regex `^/api/runs/detail/(.+)$`) `:1239`
- `GET /api/runs/:id/outline` `:1253` (`payload_preview_chars`)
- `GET /api/runs/:id/spans` `:1260` (many filter opts)
- `GET /api/runs/:id/search` `:1278` (`pattern`, `regex`, `case_sensitive`, `scope`, `span_type`, `context_chars`, `max_matches`)
- `GET /api/runs/:id/events` `:1307` (`after_id`, `types`, `limit`)
- `DELETE /api/runs/:id` `:1913`

Cloud-related (will be removed/affected by F-001):
- `POST /api/cloud/traces/import` `:1149` (localOriginGuard)
- `GET /api/query/signals` `:1105`
- `GET /api/query/events/search` `:1108`
- `GET /api/query/events` `:1111`
- `GET /api/query/traces` `:1114`
- `POST /api/import-run` `:2029`

Secrets:
- `GET /api/secrets` `:1040`
- `PUT /api/secrets/:key` `:1044`
- `DELETE /api/secrets/:key` `:1066`

Annotations:
- `GET /api/annotations` `:1841` (`run_id`)
- `POST /api/annotations` `:1850`
- `DELETE /api/annotations/:id` `:1894`

Saved runs / folders / events:
- `GET /api/saved-runs` `:1943`
- `GET /api/saved-runs/folders` `:1947`
- `POST /api/saved-runs/folders` `:1951`
- `DELETE /api/saved-runs/folders/:name` (regex) `:1958`
- `GET /api/saved-runs/events/:id` (regex) `:1964`
- `PUT /api/saved-runs/events/:id` (regex) `:1971`
- `PATCH /api/saved-runs/events/:id` (regex) `:2001`
- `DELETE /api/saved-runs/events/:id` (regex) `:2021`
- `PUT /api/saved-runs/cache/:id` (regex) `:1924`
- `GET /api/saved-runs/cache/:id` (regex) `:1929`
- `DELETE /api/saved-runs/cache/:id` (regex) `:1935`

Workspace:
- `GET /api/workspace/active` `:1330`
- `GET /api/workspace/registered` `:1339`
- `POST /api/workspace/active` `:1390`

Agent (provider-agnostic, F-002 will narrow to OpenCode):
- `GET /api/agent/provider` `:1414`
- `POST /api/agent/provider` `:1418`
- `GET /api/agent/sessions` `:1431`
- `GET /api/agent/loadout` `:1449`
- `GET /api/agent/sessions/:id` `:1455`
- `POST /api/agent/messages` `:1475` (localOriginGuard, SSE-streamed)
- `POST /api/agent-ui/commands` `:1118`

Claude-specific (removed in F-002):
- `GET /api/claude/sessions` `:1607`
- `GET /api/claude/loadout` `:1613`
- `GET /api/claude/sessions/:id` `:1622`
- `POST /api/claude/ask-user-question/hook` `:1633`
- `POST /api/claude/ask-user-question/:id/answer` `:1646`
- `POST /api/claude/messages` `:1659` (localOriginGuard)

Agent registry / replay:
- `GET /api/agents` `:2087`
- `PUT /api/agents` `:2092`
- `POST /api/agents/refresh` `:2104`
- `GET /api/agents/health` `:2133`
- `POST /api/agents/ask` `:2137`
- `POST /api/replay/context` `:2263`
- `POST /api/replay` `:2277` (SSE)

UI catch-all:
- `GET *` `:2312` (serves built Vite SPA from `builtAppDir`)

Cloud MCP proxy mount: `app.use(CLOUD_MCP_PROXY_PATH, ...)` at `:690-696` (constant from `src/cloud/cloud-mcp-proxy.ts`).

## 1.3 WebSocket event catalog

WS server: `wss = new WebSocketServer({ server, path: "/ws", verifyClient })` at `src/server.ts:413-424`. Loopback-only guard.

Connection handler at `:604-632`. On connect, server immediately pushes:
- `claude_loadout` `:608`
- `agent_provider` `:610`
- `agent_loadout` `:613` (if active workspace)
- `claude_ask_user_question` (for each pending question) `:616`

Client→server (only one inbound type):
- `ui_view` message `{ type: "ui_view", run_id, span_id }` `:622-626` (handled by `viewingRegistry.update`)

Server→client broadcasts (search `broadcast("..."`):
- `claude_loadout` `:592`
- `agent_loadout` `:593`
- `spans` `:812, :976, :1160, :1916, :2056` (new/updated run IDs)
- `live` `:861` (live event: traceId, spanId, type, content, timestamp, metadata)
- `secrets_updated` `:1053, :1062, :1073`
- `annotation` `:1138, :1878, :1897` (insert or delete)
- `agent_ui_command` `:1146, :1162` (navigate_to_run, compose_annotation)
- `clear` `:1328`
- `workspace_changed` `:1407`
- `agent_provider` `:1425`
- `agent_message_stream` `:1522`
- `claude_message_stream` `:1523, :1688`
- `agents_updated` `:2097, :2107`

JSON wire format: `{ event: string, data: any }` (`:429`). Inbound parsed via `JSON.parse(raw.toString())`.

## 1.4 SQLite tables (`src/db/schema.ts`)

| Table | Lines | Purpose |
|---|---|---|
| `runs` | `:4-21` | One per OTLP trace. PK `id`. Cols: id, event_id, name, event_name, user_id, convo_id, started_at (NOT NULL int), last_updated_at (NOT NULL int), metadata. Indexes: `idx_runs_last_updated`, `idx_runs_event_id`. |
| `spans` | `:23-47` | PK `id`. FK `run_id → runs.id` (NOT NULL). Cols: parent_span_id, name (NOT NULL), span_type, status (default `"UNSET"`), input_payload, output_payload, start_time_ms (real), end_time_ms (real), duration_ms (real), model, provider, input_tokens, output_tokens, attributes (text). Indexes: `idx_spans_run_id`, `idx_spans_parent`. |
| `live_events` | `:49-61` | PK `id` (autoIncrement). Cols: trace_id (NOT NULL), span_id, type (NOT NULL), content, timestamp (NOT NULL int), metadata. Index: `idx_live_trace(trace_id, timestamp)`. |
| `saved_run_cache` | `:63-66` | PK `id`. Cols: data (text NOT NULL). |
| `saved_events` | `:68-89` | PK `id`. Cols: event_name (NOT NULL), user_id, convo_id, timestamp (text NOT NULL), user_input, assistant_output, signals, properties, saved_at (int NOT NULL), summary, source, folder. Indexes: `idx_saved_events_saved_at` (desc), `idx_saved_events_folder`. |
| `saved_folders` | `:91-95` | PK `name`. Cols: color (NOT NULL), created_at (int NOT NULL). |
| `sessions` | `:97-106` | PK `id`. Cols: active (int NOT NULL), created_at (int NOT NULL), deactivated_at. Index: `idx_sessions_active_created(active, created_at desc)`. |
| `messages` | `:108-124` | PK `id`. FK `session_id → sessions.id` (NOT NULL). Cols: role enum `user|assistant` (NOT NULL), content (NOT NULL), run_id, state enum `pending|delivered|processing|done|error|timeout` (NOT NULL), created_at, state_updated_at. Indexes: `idx_messages_session_created`, `idx_messages_state`. |
| `annotations` | `:126-141` | PK `id`. Cols: run_id (NOT NULL), span_id, kind enum `issue|good|note` (NOT NULL), note, source enum `user|claude-code|codex` (NOT NULL), created_at (int NOT NULL). Indexes: `idx_annotations_run`, `idx_annotations_span` (where span_id IS NOT NULL). |
| `runs_with_hints` (VIEW) | `:143-170` | Joins `runs` with subqueries on `spans` and `live_events`. Adds columns: model, finished, span_count, live_event_count, payload_total_chars. |

Drizzle helpers imported at `:1-2`. No real FKs declared beyond `run_id` and `session_id` references.

## 1.5 `package.json` scripts

From `package.json` `"scripts"` block (lines 17-41):

| Name | Command | When |
|---|---|---|
| `build` | `bun scripts/embed-migrations.ts --check && bun x tsc --noEmit && bun run build:ui` | Release typecheck + UI build. |
| `build:ui` | `cd app && bun x vite build` | Build the React UI into `app/dist`. |
| `build:bun` | `bun scripts/build-bun.ts` | Build the `raindrop` Bun binary. |
| `build:bun:all` | `bun scripts/build-bun.ts --all` | Build all platform binaries. |
| `dev` | `bun run link-dev && bun --watch src/index.ts workshop serve & (printf "..."; cd app && bun x vite --force) & wait` | Start daemon + Vite UI together (parallel). |
| `link-dev` | `ln -sf ../bin/raindrop-dev node_modules/.bin/raindrop-dev` | Make `raindrop-dev` shim available. |
| `postinstall` | `bun run link-dev` | Auto-link after `bun install`. |
| `dev:server` | `bun --watch src/index.ts workshop serve` | Daemon only (foreground). |
| `dev:ui` | `cd app && bun x vite --force` | Vite dev server only (UI without backend). |
| `dev:examples` | `bun scripts/dev-all.ts` | Run example apps. |
| `db:generate` | `drizzle-kit generate && bun scripts/embed-migrations.ts` | Generate SQL from schema; embed migrations. |
| `db:embed` | `bun scripts/embed-migrations.ts` | Re-embed existing migration SQL. |
| `db:migrate` | `drizzle-kit migrate` | Apply migrations (dev only). |
| `db:studio` | `drizzle-kit studio` | Drizzle Studio UI. |
| `setup` | `bash .devin/setup.sh` | Devin CI setup. |
| `seed:traces` | `bun scripts/seed-traces.ts` | Seed DB with sample traces. |
| `start` | `bun src/index.ts workshop serve` | Foreground daemon (no watch). |
| `test` | `bun test tests/` | Run tests (WARNING: `tests/` dir does not exist). |
| `test:watch` | `bun test tests/ --watch` | Watch mode tests (also broken). |
| `lint` | `eslint .` | Lint everything. |
| `lint:fix` | `eslint . --fix` | Autofix lint. |
| `manifest` | `bun scripts/generate-manifest.ts` | Regen self-update manifest. |
| `install:local` | `bun scripts/install-local.ts` | Local install hook. |

Engines: `bun >=1.3.13` (`package.json` lines 11-13).

## 1.6 Other anchors

- `drizzle.config.ts` — Drizzle config; points at `src/db/schema.ts`; output dir `drizzle/`.
- `eslint.config.mjs` — ESLint flat config (lint command source).
- Env vars referenced in source: `RAINDROP_WORKSHOP_PORT` (`src/index.ts:58`), `RAINDROP_WORKSHOP_DB_PATH` (db path override), `RAINDROP_LOCAL_DEBUGGER` (SDK→daemon URL; written by `raindrop workshop setup` `src/index.ts:451`), `RAINDROP_WORKSHOP_BIND_HOST` (`src/index.ts:59`), `RAINDROP_WORKSHOP_URL` (`src/index.ts:112`), `RAINDROP_WORKSHOP_CLAUDE_CLI_CHAT` (`src/server.ts:441`), `RAINDROP_WORKSHOP_ALLOWED_ORIGINS` (`src/server.ts:444`), `RAINDROP_WORKSHOP_UI_PORT` (default 5900, used in `dev` script), `RAINDROP_MANIFEST_URL` (update manifest), `RAINDROP_QUERY_API_KEY` / `RAINDROP_WRITE_KEY` (cloud — to be removed in F-001), `DEBUGGER_DEV` (skip static UI mount `src/server.ts:2309`).
- `raindrop.span.kind` enum (`src/parse.ts:47-50`): `agent_root`, `trace`, `llm_call`, `tool_call`.
- Span types (`src/parse.ts:41`): `LLM_GENERATION`, `TOOL_CALL`, `AGENT_ROOT`, `TRACE`, `INTERNAL`.
- OTLP proto definition (`src/otlp-protobuf.ts:3-94`): standard OTLP `ExportTraceServiceRequest` with `ResourceSpans → ScopeSpans → Span`.
- Ingest base URL: `http://localhost:5899/v1/` (default port 5899, path `/v1/traces` etc).
- `task` sub-agent detection: `src/agents.ts` keys off `span.name === "agent.subagent"` (`:78-81`) and `span_type` `TOOL_CALL`/`LLM_GENERATION`. No attribute name lookup for `subagent_name` — name comes from the parent TOOL_CALL span's `name` field.
- Existing `ai-docs/AGENTS.md` Quick Reference table at `ai-docs/AGENTS.md:85-95`. New rows must be appended to that table.
